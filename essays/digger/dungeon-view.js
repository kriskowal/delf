"use strict";

module.exports = DungeonView;
// box is a property of scope
// document is a property of slot
// template is component constructors for each named argument
function DungeonView(slot, scope, argument, attributes) {
}

DungeonView.prototype.handleResize = function () {
};

